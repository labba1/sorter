#include <iostream>
#include <set>
#include <algorithm>
using namespace std;
/*
template <typename T>
void sortWithVector(const vector<T> data,int m) {
    sort(data.begin(), data.end());
  cout << "Sorted by vector: ";
    for (const T& x : data) {
        cout << x << " ";
    }
    cout << endl;
}*/

template <typename T>
void sortWithSet(T arr[], int n) {
    set<T> sortedSet(arr, arr + n);
    cout << "The sorted elements are : ";
    for (T x : sortedSet) {
        cout << x << " ";
    }
    cout << endl;
}

int main() {

    int n;
    cout << "Enter n";
    cin >> n;

    int numbers[30];
    cout << "Enter " << n << "elements : ";

    vector<double> numbers;
    cout << "Enter " << n << " numbers: ";
     for (int i = 0; i < n; i++) {
        double val;
        cin >> val;
        numbers.push_back(val);
    }

    sortWithVector(numbers, n);
    sortWithSet(numbers, n);

    int m;
    cout << "\n enter m ";
    cin >> m;

    string words[30];
    cout << "Enter " << m << " words: ";
    for (int i = 0; i < m; i++) {
        cin >> words[i];
    }

    //sortWithVector(words, m);
    sortWithSet(words, m);

    return 0;
}
